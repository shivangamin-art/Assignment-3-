<!--
  todos/list.ejs
  display the user's tasks in a table with quick actions: complete, edit, delete.
-->
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>TaskTrackr - My Tasks</title>

  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="/css/styles.css">
</head>
<body class="bg-light">

  <!-- navbar -->
  <nav class="navbar navbar-dark bg-dark mb-4">
    <div class="container d-flex justify-content-between align-items-center">
      <a class="navbar-brand" href="/">TaskTrackr</a>
      <a class="nav-link text-light" href="/todos">All Tasks</a>
      <a class="nav-link text-light" href="/lists">Lists</a>
    </div>
  </nav>

  <main class="container py-4">

    <!-- header for page title and action buttons -->
    <div class="d-flex justify-content-between align-items-center mb-4">
      <h2>My Tasks</h2>

      <div class="d-flex gap-2">
        <!-- buttion for hide complete/show complet tasks -->
        <a
          href="<%= showCompleted ? '/todos' : '/todos?showCompleted=true' %>"
          class="btn btn-outline-secondary"
        >
          <%= showCompleted ? 'Hide completed' : 'Show completed' %>
        </a>

        <!--  create a new task -->
        <a href="/todos/new" class="btn btn-success">Add Task</a>
      </div>
    </div>

    <!-- tasks table -->
    <table class="table table-striped table-hover align-middle">
      <thead>
        <tr>
          <th>Title</th>
          <th>List</th>
          <th>Due Date</th>
          <th>Priority</th>
          <th>Status</th>
          <th>Important</th>
          <th style="width: 220px;">Actions</th>
        </tr>
      </thead>
      <tbody>
        <!-- empty task message -->
        <% if (todos.length === 0) { %>
          <tr><td colspan="7" class="text-center text-muted py-4">No tasks</td></tr>
        <% } %>

        <!-- rendering to each task row -->
        <% todos.forEach(todo => { %>
          <tr class="<%= todo.completed ? 'table-success' : '' %>">
            <!-- Title and basic details -->
            <td><%= todo.title %></td>
            <td><%= todo.list ? todo.list.name : '-' %></td>
            <td><%= todo.dueDate ? todo.dueDate.toDateString() : '-' %></td>
            <td><%= todo.priority %></td>
            <td><%= todo.completed ? 'Done' : 'Pending' %></td>
            <td><%= todo.important ? '⭐' : '-' %></td>

            <!-- Action buttons: complete, edit, delete -->
            <td>
              <% if (!todo.completed) { %>
                <form action="/todos/<%= todo._id %>/complete?_method=PUT" method="POST" class="d-inline">
                  <button class="btn btn-sm btn-success">Complete</button>
                </form>
              <% } %>

              <a href="/todos/<%= todo._id %>/edit" class="btn btn-sm btn-primary">Edit</a>
              <a href="/todos/<%= todo._id %>/delete" class="btn btn-sm btn-outline-danger">Delete</a>
            </td>
          </tr>
        <% }) %>
      </tbody>
    </table>

  </main>

  <!-- footer for copyright note -->
  <footer class="text-center py-3 mt-5 border-top bg-white">
    <small>&copy; <%= new Date().getFullYear() %> TaskTrackr</small>
  </footer>

</body>
</html>
